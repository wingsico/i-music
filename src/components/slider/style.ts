import styled from "styled-components";
import globalStyle from "@/assets/style/global-style";

export const ActiveBulletWrapper = styled.div`
  .swiper-pagination-bullet-active {
    background: ${globalStyle["theme-color"]};
  }
`;
